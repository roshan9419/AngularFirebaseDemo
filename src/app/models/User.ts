export interface User {
    userId?: string,
    name?: string,
    email?: string,
    photoUrl?: string,
    registeredDate?: Date,
    occupation?: string,
    aboutMe?: string,
    githubUsername?: string
}